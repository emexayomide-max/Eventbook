services:
  postgres:
    image: postgres:16
    container_name: eventbook-postgres
    environment:
      POSTGRES_DB: eventbook
      POSTGRES_USER: eventbook_user
      POSTGRES_PASSWORD: change_me
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: